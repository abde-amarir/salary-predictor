# Virtual environment — never commit this!
venv/

# Python cache files — auto-generated, not needed
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Jupyter notebook checkpoints
.ipynb_checkpoints/

# Data files — often large, sometimes sensitive
# Raw data — large files, never commit
data/raw/
*.zip

# Keep these SPECIFIC processed files out of ignore
# (We need them for deployment!)
!data/processed/best_model.pkl
!data/processed/label_encoders.pkl
!data/processed/survey_engineered.csv
!data/processed/survey_clean.csv

# OS files
.DS_Store
Thumbs.db

# IDE settings
.vscode/
.idea/